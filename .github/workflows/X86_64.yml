name: 构建并发布 luci-app-openvpn-admin (X86_64版)

on:
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (例如: v1.0.0)'
        required: true
        default: 'v1.0.0'
      prerelease:
        description: '是否为预发布版本'
        required: true
        default: false
        type: boolean
        

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 180
    env:
      MAKE_JOBS: 4  # 使用英文变量名
      SDK_URL: "https://downloads.immortalwrt.org/releases/23.05.0/targets/x86/64/immortalwrt-sdk-23.05.0-x86-64_gcc-12.3.0_musl.Linux-x86_64.tar.xz"
      DEPENDENCIES: "openssl-util curl openvpn-openssl luci-lib-jsonc luci-compat luci-base"

    steps:
      - name: 1. 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 2. 安装编译依赖
        run: |
          sudo apt-get update && sudo apt-get install -y build-essential libncurses5-dev gawk git wget unzip python3 python3-pip file rsync curl python3-pyelftools subversion time
          sudo pip3 install pyelftools || true

      - name: 3. 下载并解压 ImmortalWrt SDK
        id: sdk
        run: |
          wget --timeout=60 --tries=3 $SDK_URL -O sdk.tar.xz || { echo "SDK下载失败"; exit 1; }
          tar xf sdk.tar.xz && mv $(find . -maxdepth 1 -type d -name "*sdk*" | head -1) openwrt-sdk
          echo "SDK目录内容:"
          ls -la openwrt-sdk/

      - name: 4. 准备插件文件
        run: |
          cd openwrt-sdk
          rm -rf package/luci-app-openvpn-admin && mkdir -p package/luci-app-openvpn-admin
          [ -f ../Makefile ] && cp ../Makefile package/luci-app-openvpn-admin/
          [ -d ../luasrc ] && cp -r ../luasrc package/luci-app-openvpn-admin/
          [ -d ../root ] && cp -r ../root package/luci-app-openvpn-admin/
          if [ -d ../files ]; then
            mkdir -p package/luci-app-openvpn-admin/root
            cp -r ../files/* package/luci-app-openvpn-admin/root/
          fi
          echo "==== 插件目录结构 ===="
          find package/luci-app-openvpn-admin -type d | sort | head -20

      - name: 5. 更新软件源并安装依赖
        run: |
          cd openwrt-sdk
          ./scripts/feeds update -a -f
          ./scripts/feeds install luci-base luci-lib-jsonc luci-compat openvpn-openssl curl openssl-util || true
          echo "==== 已安装的软件源 ===="
          ./scripts/feeds list | grep -E "luci|openvpn|curl"

      - name: 6. 生成编译配置
        run: |
          cd openwrt-sdk
          cat > .config << EOF
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_DEVICE_generic=y
          CONFIG_PACKAGE_luci-app-openvpn-admin=y
          EOF
          make defconfig
          echo "==== 配置摘要 ===="
          grep -E "CONFIG_TARGET|CONFIG_PACKAGE|CONFIG_OPENVPN" .config

      - name: 7. 编译依赖包
        run: |
          cd openwrt-sdk
          for pkg in $DEPENDENCIES; do
            echo "==== 正在编译 $pkg ===="
            pkg_path=$(find package feeds -name $pkg -type d | head -1)
            if [ -n "$pkg_path" ]; then
              make package/$pkg/compile V=sc -j$MAKE_JOBS || echo "⚠ $pkg 编译失败，跳过（非关键依赖）"
            else
              echo "⚠ $pkg 在软件源中未找到，跳过"
            fi
          done

      - name: 8. 编译主插件包
        run: |
          cd openwrt-sdk
          [ ! -d "package/luci-app-openvpn-admin" ] && { echo "插件目录不存在"; exit 1; }
          
          # 清理旧构建文件
          make package/luci-app-openvpn-admin/clean
          
          # 编译并输出详细日志
          echo "开始编译主插件，详细日志如下："
          make package/luci-app-openvpn-admin/compile V=s 2>&1 | tee compile-log.txt
          
          # 检查编译结果（等待一段时间确保编译完成）
          sleep 5
          
          # 查找IPK文件
          if find . -name "*luci-app-openvpn-admin*.ipk" -type f 2>/dev/null | grep -q .; then
            MAIN_IPK=$(find . -name "*luci-app-openvpn-admin*.ipk" -type f 2>/dev/null | head -1)
            echo "✅ 主插件编译成功: $MAIN_IPK"
          else
            echo "==== 编译日志（最后100行） ===="
            tail -100 compile-log.txt
            echo "❌ 主插件编译失败"
            exit 1
          fi

      - name: 9. 收集并准备发布文件
        id: prepare-release
        run: |
          cd openwrt-sdk
          
          # 查找主插件IPK
          MAIN_IPK=$(find . -name "*luci-app-openvpn-admin*.ipk" -type f 2>/dev/null | head -1)
          
          if [ -z "$MAIN_IPK" ]; then
            echo "❌ 未找到主插件IPK文件"
            exit 1
          fi
          
          # 获取IPK详细信息
          IPK_NAME=$(basename "$MAIN_IPK")
          IPK_SIZE=$(du -h "$MAIN_IPK" | cut -f1)
          
          # 提取版本信息
          if tar -xzOf "$MAIN_IPK" ./control.tar.gz 2>/dev/null | tar -xzO ./control 2>/dev/null > /tmp/control; then
            PKG_VERSION=$(grep -i "^Version:" /tmp/control | cut -d' ' -f2)
            PKG_ARCH=$(grep -i "^Architecture:" /tmp/control | cut -d' ' -f2)
          else
            PKG_VERSION="未知"
            PKG_ARCH="未知"
          fi
          
          # 创建发布目录
          mkdir -p ../release-artifacts
          cp "$MAIN_IPK" ../release-artifacts/
          cp compile-log.txt ../release-artifacts/编译日志.txt
          
          # 创建说明文件
          cat > ../release-artifacts/README.md << EOF
          # luci-app-openvpn-admin - X86_64 版本
          
          ## 插件信息
          - **文件名**: ${IPK_NAME}
          - **版本**: ${PKG_VERSION}
          - **架构**: ${PKG_ARCH}
          - **大小**: ${IPK_SIZE}
          - **编译时间**: $(date '+%Y-%m-%d %H:%M:%S')
          - **GitHub Actions ID**: ${{ github.run_id }}
          
          ## 安装方法
          
          \`\`\`bash
          # 上传到路由器
          scp ${IPK_NAME} root@路由器IP:/tmp/
          
          # 安装
          opkg install /tmp/${IPK_NAME}
          
          # 如果依赖缺失，先安装依赖
          opkg install curl openvpn-openssl openssl-util luci-lib-jsonc luci-compat
          \`\`\`
          
          ## 支持的固件
          - ImmortalWrt 23.05.0
          - OpenWrt 23.05.x
          - 其他基于musl的x86_64固件
          
          ## 注意事项
          1. 确保路由器有足够的空间
          2. 安装后需要重启luci界面或路由器
          3. 配置前请确保已安装并配置好OpenVPN
          EOF
          
          # 输出信息供后续步骤使用
          echo "IPK_PATH=$MAIN_IPK" >> $GITHUB_OUTPUT
          echo "IPK_NAME=$IPK_NAME" >> $GITHUB_OUTPUT
          echo "PKG_VERSION=$PKG_VERSION" >> $GITHUB_OUTPUT
          echo "PKG_ARCH=$PKG_ARCH" >> $GITHUB_OUTPUT
          
          echo "✅ 发布文件准备完成"
          echo "📦 文件: $IPK_NAME"
          echo "🏷️ 版本: $PKG_VERSION"
          echo "🏗️ 架构: $PKG_ARCH"

      - name: 10. 创建GitHub Release
        id: create-release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.event.inputs.version }}
          name: luci-app-openvpn-admin ${{ github.event.inputs.version }} (X86_64)
          body: |
            # luci-app-openvpn-admin - X86_64 版本
            
            ## 版本信息
            - **版本**: ${{ github.event.inputs.version }}
            - **编译时间**: $(date -u +'%Y-%m-%dT%H:%M:%SZ')
            - **架构**: ${{ steps.prepare-release.outputs.PKG_ARCH }}
            - **插件版本**: ${{ steps.prepare-release.outputs.PKG_VERSION }}
            - **是否为预发布版本**: ${{ github.event.inputs.prerelease }}
            
            ## 安装说明
            
            1. 下载对应的IPK文件
            2. 上传到路由器:
            ```bash
            scp ${{ steps.prepare-release.outputs.IPK_NAME }} root@路由器IP:/tmp/
            ```
            
            3. 安装插件:
            ```bash
            opkg install /tmp/${{ steps.prepare-release.outputs.IPK_NAME }}
            ```
            
            ## 依赖要求
            - openssl-util
            - curl
            - openvpn-openssl
            - luci-lib-jsonc
            - luci-compat
            - luci-base
            
            ## 支持的固件
            - ImmortalWrt 23.05.0 (x86_64)
            - OpenWrt 23.05.x (x86_64)
            
            ## 源码
            源码仓库: https://github.com/${{ github.repository }}
          draft: false
          prerelease: ${{ github.event.inputs.prerelease }}
          generate_release_notes: true
          files: |
            release-artifacts/${{ steps.prepare-release.outputs.IPK_NAME }}
            release-artifacts/README.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 11. 上传构建产物（备份）
        uses: actions/upload-artifact@v4
        with:
          name: openvpn-admin-x86_64-${{ github.run_id }}
          path: |
            release-artifacts/
            openwrt-sdk/compile-log.txt
          retention-days: 30

      - name: 12. 清理工作空间（缓存和中间文件）
        if: always()
        run: |
          echo "==== 开始清理工作空间 ===="
          echo "清理前磁盘使用情况:"
          df -h
          echo "清理前工作空间大小:"
          du -sh . || true
          if [ -d "openwrt-sdk" ]; then
            echo "清理OpenWrt SDK中间文件..."
            cd openwrt-sdk
            rm -rf build_dir 2>/dev/null || true
            rm -rf staging_dir 2>/dev/null || true
            rm -rf tmp 2>/dev/null || true
            rm -rf logs 2>/dev/null || true
            cd ..
          fi
          echo "清理APT缓存..."
          sudo apt-get clean 2>/dev/null || true
          sudo rm -rf /var/lib/apt/lists/* 2>/dev/null || true
          echo "清理pip缓存..."
          pip3 cache purge 2>/dev/null || true
          echo "清理临时文件..."
          sudo rm -rf /tmp/* 2>/dev/null || true
          sudo rm -rf /var/tmp/* 2>/dev/null || true
          rm -f sdk.tar.xz 2>/dev/null || true
          echo "保留release-artifacts目录，清理其他..."
          if [ -d "release-artifacts" ]; then
            mkdir -p /tmp/release-backup
            cp -r release-artifacts/* /tmp/release-backup/ 2>/dev/null || true
            find . -maxdepth 1 ! -name "release-artifacts" ! -name "." -exec rm -rf {} + 2>/dev/null || true
            mv /tmp/release-backup/* release-artifacts/ 2>/dev/null || true
            rm -rf /tmp/release-backup
          fi
          echo "清理后磁盘使用情况:"
          df -h
          echo "清理后工作空间大小:"
          du -sh . 2>/dev/null || true
          echo "✅ 工作空间清理完成"

      - name: 13. 上传最终产物（清理后）
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: 最终产物-${{ github.run_id }}
          path: |
            release-artifacts/
          retention-days: 7

      - name: 14. 生成构建报告
        if: always()
        run: |
          echo "========================================"
          echo "构建状态报告 - X86_64版本"
          echo "========================================"
          echo "作业状态: ${{ job.status }}"
          echo "触发方式: 手动触发"
          echo "工作流ID: ${{ github.run_id }}"
          echo "运行编号: ${{ github.run_number }}"
          echo "版本号: ${{ github.event.inputs.version }}"
          echo "预发布版本: ${{ github.event.inputs.prerelease }}"
          echo ""
          echo "📊 磁盘使用情况:"
          df -h | grep -E "Filesystem|/dev/"
          echo ""
          if [ -d release-artifacts ]; then
            echo "📦 发布文件列表:"
            ls -lh release-artifacts/
            echo ""
            echo "🔧 插件信息:"
            if [ -f release-artifacts/README.md ]; then
              grep -E "文件名|版本|架构|大小" release-artifacts/README.md | head -4
            fi
          else
            echo "❌ 发布目录不存在"
          fi
          echo ""
          echo "========================================"
          echo "构建完成 - 请前往Releases页面下载文件"
          echo "========================================"
